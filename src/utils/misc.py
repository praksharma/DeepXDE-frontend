# def set_png_icon():
#     """
#     Set the icon of the application window to the .png file specified in the path.
#     """
#     icon_path = os.path.join(os.getcwd(), "assets", "icon.png")
#     root.iconphoto(True, tk.PhotoImage(file=icon_path))  # True: Set the icon for the window, not just the tk object.
#     # root.iconbitmap(icon_path)  # Alternative method to set the icon of the window.